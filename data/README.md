# Data

No external dataset is required for this paper experiment package.

The simulator generates task-offloading samples from configured scenario seeds, task profiles, MMPP arrivals, privacy levels, edge/cloud node capacities, link noise, and seen/unseen distribution shifts.

Use `data/raw/` and `data/processed/` only for generated scenario manifests or cached artifacts.

